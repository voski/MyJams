<h1>All Bands</h1>
<ol>
  <% @bands.each do |band| %>
  <li>
    <%= link_to "#{band.name}", band_url(band) %>
  </li>
  <% end %>
</ol>
<div class="nav_bar">
  <%= link_to "new band", new_band_url , method: :get %>
</div>
